name: build-and-test

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  PipelineEnvironment: true
  NUGET_XMLDOC_MODE: skip

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Restore tests
        run: dotnet restore test/Soenneker.Compression.Tar.XZ.Tests/Soenneker.Compression.Tar.XZ.Tests.csproj --verbosity minimal

      - name: Build tests
        run: dotnet build test/Soenneker.Compression.Tar.XZ.Tests/Soenneker.Compression.Tar.XZ.Tests.csproj --configuration Release --no-restore --verbosity minimal

      - name: Test
        run: dotnet test test/Soenneker.Compression.Tar.XZ.Tests/Soenneker.Compression.Tar.XZ.Tests.csproj --configuration Release --no-build --no-restore --verbosity minimal

      - name: Pack
        run: dotnet pack src/Soenneker.Compression.Tar.XZ/Soenneker.Compression.Tar.XZ.csproj --configuration Release --no-build --no-restore --output . --verbosity minimal
